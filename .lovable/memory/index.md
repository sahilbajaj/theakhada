# Lovable Memory

CourtSync Club should remain import-ready for Lovable, locally runnable with `npm run dev`, and deployable as a static Vite app on AWS Amplify.

Use Supabase as the backend. Keep local demo data available so first-run previews do not render empty or broken screens before environment variables are configured.
